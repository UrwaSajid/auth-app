{
  "name": "auth-app",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "mocha tests/**/*.test.js --timeout 10000",
    "test:unit": "mocha tests/unit/**/*.test.js --timeout 10000",
    "test:integration": "mocha tests/integration/**/*.test.js --timeout 10000",
    "test:report": "mocha tests/**/*.test.js --reporter json > test-results.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "ejs": "^5.0.2",
    "express": "^5.2.1",
    "express-session": "^1.19.0"
  },
  "devDependencies": {
    "chai": "^4.3.10",
    "mocha": "^11.7.5"
  }
}
